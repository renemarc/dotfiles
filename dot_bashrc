# -*-mode:bash-*- vim:ft=bash

#
# Bash: Non-login shell configuration
#

# Load split configuration for easier maintenance
for file in "$(dirname "$BASH_SOURCE")"/.bash/{shell,prompt,aliases,functions,extra}; do
    [ -r "$file" ] && source "$file";
done;
unset file;
